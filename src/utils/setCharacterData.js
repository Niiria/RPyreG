const CHARACTER_INFO_NAV = document.querySelector('.characterInfo_nav');
const CHARACTER_INFO_IMG = document.querySelector('.characterInfo_img');
const CHARACTER_INFO_DESRIPTION_TEXT = document.querySelector(
  // eslint-disable-next-line comma-dangle
  '.characterInfo_description-text'
);
const CHARACTER_INFO_MAIN_LEFT_LIST = document.querySelector(
  // eslint-disable-next-line comma-dangle
  '.characterInfo_main-left-list'
);
const CHARACTER_INFO_MAIN_RIGHT_LIST = document.querySelector(
  // eslint-disable-next-line comma-dangle
  '.characterInfo_main-right-list'
);

const setCharacterData = (nav, img, text, leftList, rightList) => {};
